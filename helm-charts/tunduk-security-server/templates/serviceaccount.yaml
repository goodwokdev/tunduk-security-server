{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tunduk.serviceAccountName" . }}
  labels:
    {{- include "tunduk.labels" . | nindent 4 }}
{{- end }}
